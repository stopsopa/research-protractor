{
  "dependencies": {
    "protractor": "^5.1.2"
  },
  "scripts": {
    "status": "curl localhost:4444 &> /dev/null && echo 'local' || echo 'public'",
    "start": "/bin/bash cli.sh",
    "stop": "/bin/bash cli.sh stop"
  }
}
